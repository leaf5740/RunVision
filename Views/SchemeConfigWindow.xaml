<Window x:Class="RunVision.Views.SchemeConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:local="clr-namespace:RunVision.Views" 
        xmlns:views="clr-namespace:RunVision.Views.TabViews"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="方案配置" Height="700" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <hc:TabControl 
                       Grid.Row="1"
                       ShowContextMenu="False" 
                       IsAnimationEnabled="True" 
                       ShowCloseButton="False" 
                       IsDraggable="True" 
                       IsTabFillEnabled="True">

            <hc:TabItem Header="相机配置">
                <views:CameraTabView/>
            </hc:TabItem>

            <hc:TabItem Header="PLC配置">
                <views:PlcTabView/>
            </hc:TabItem>

            <hc:TabItem Header="数据库配置">
                <views:DatabaseTabView/>
            </hc:TabItem>

            <hc:TabItem Header="Vm方案配置">
                <views:SolutionTabView/>
            </hc:TabItem>

            <hc:TabItem Header="存图配置">
                <views:SaveImageTabView/>
            </hc:TabItem>

        </hc:TabControl>
    </Grid>
</Window>
